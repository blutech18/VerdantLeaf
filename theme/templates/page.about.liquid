<section class="section section--dark" style="min-height: 70vh; display: flex; align-items: center; justify-content: center; text-align: center; padding: var(--space-2xl) var(--space-md);">
  <div class="container container--narrow">
    <span class="section-label">Our Story</span>
    <h1 class="section-title" style="color: var(--color-cream); font-size: clamp(2.5rem, 4vw, 4rem); margin-bottom: var(--space-lg);">
      Verdant Leaf
    </h1>
    <p style="color: var(--color-sage-light); font-size: var(--text-lg); line-height: 1.6; max-width: 600px; margin: 0 auto var(--space-2xl);">
      We are dedicated to sourcing the finest artisan teas from small, sustainable farms across the globe. Our mission is to connect tea lovers with the rich history, culture, and pure flavors of single-origin teas, curated with care and passion.
    </p>
    <a href="{{ routes.all_products_collection_url }}" class="btn btn--gold btn--lg">
      Shop Our Teas
    </a>
  </div>
</section>
