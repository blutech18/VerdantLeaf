<section class="section">
  <div class="container container--narrow">
    <h1 class="page-title text-center" style="margin-bottom: var(--space-xl);">{{ page.title }}</h1>
    <div class="page-content">
      {{ page.content }}
    </div>
  </div>
</section>
